/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['images.pexels.com', 'api.multiavatar.com', 'images.unsplash.com'],
  },
}