import React, { FC } from 'react';
// types
// components
// context
// styles
import styles from '../../styles/css/logo.module.css';



const Logo:FC = () => {
  return (
    <header>
    </header>
  );
};

export default Logo;