import React, { FC } from 'react';
// types
// components
// context
// styles
import styles from '../../styles/css/header.module.css';



const Header:FC = () => {
  return (
    <header>
    </header>
  );
};

export default Header;